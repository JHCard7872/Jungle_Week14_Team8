-- ==========================================================================
-- TruckData — 수거 트럭 설정 상수 (require 모듈, 읽기 전용)
-- [역할] 트럭 주행/사운드 튜닝값과 트랙(웨이포인트) 정의.
-- [사용법] local Truck = require("Data/TruckData")
-- [특이사항] 트랙 조절 = waypoints만 수정. TruckBehavior가 매 Tick require하므로
--            저장하면 핫리로드로 반영된다 (캐시가 안 비워지는 환경이면 다음 판부터).
--            트랙 모양은 아직 미정 — 아래는 임시 사각형.
-- ==========================================================================

return {
    speed          = 5.0,   -- 전진 속도(m/s)               -- 임시 밸런스
    turnRate       = 90,    -- 조향 속도(도/초)              -- 임시 밸런스
    arriveDistance = 2.0,   -- 웨이포인트 도착 판정 반경(XY 평면)
    waitTime       = 5,    -- 순회 사이 대기 시간(초)        -- 임시 밸런스

    engineVolume   = 0.5,   -- BgmTruck 루프 볼륨

    waypoints = {           -- 순서대로 순회, 마지막 → 처음으로 순환 (후진 없음)
        { -15, -15, 0 },
        {  15, -15, 0 },
        {  15,  15, 0 },
        { -15,  15, 0 },
    },
}
