-- ============================================================================
-- PortalData — 소환 포탈 튜닝값 (require 모듈, 읽기 전용)
-- [역할] 포탈 재배치 좌표 풀과 흡수 연출 파라미터. 비주얼(메시/파티클)은 C++ 액터가
--        쥐고, 여기 값은 PortalBehavior.lua가 읽는 "배선값"이다.
-- [사용법] local Portal = require("Data/PortalData")
-- [특이사항] spawnPositions는 맵 좌표라 실측이 필요하다 — 아래는 플레이스홀더.
--            포탈은 미션이 새로 발급될 때마다 이 중 하나로 순간이동한다.
-- ============================================================================

return {
    -- 미션 발급마다 이 좌표 중 하나로 이동 (z는 포탈 바닥 높이). 맵 미상 → 실측 교체 필요
    spawnPositions = {
        { x = 12.0,  y = 8.0,   z = 0.0 },
        { x = -14.0, y = 6.0,   z = 0.0 },
        { x = 4.0,   y = -16.0, z = 0.0 },
        { x = -10.0, y = -12.0, z = 0.0 },
        { x = 18.0,  y = -2.0,  z = 0.0 },
    },

    shrinkDuration    = 1.0,  -- 흡수 시 래그돌이 줄어드는 시간(초)
    shrinkTargetRatio = 0.1,  -- 흡수 끝 스케일 비율 (1.0 → 0.1)

    collectFxPath = "Content/Data/Buble.uasset", -- 흡수 파티클 — 추후 FX_CollectPop으로 교체
    collectFxLife = 1.0,                          -- 파티클 자동 정리 수명(초)
}
