-- =========================================================================
-- ScoreData — 점수 공식 상수 (require 모듈, 읽기 전용)
-- [역할] 금/은 변형 배수·발생 확률, 종료 시 보너스/감점 정의. ScoreManager와
--        스폰 담당(금/은 추첨)이 읽는다.
-- [사용법] local Score = require("Data/ScoreData")
-- [특이사항] 금/은 래그돌(기믹: 값비싼 래그돌)은 스폰 시 "Gold"/"Silver" 태그로
--            구분된다. timeBonus/overloadPenalty의 적용 시점은 기획 미정 — 값만 정의.
-- =========================================================================

return {
    goldMultiplier   = 3.0,   -- 금 래그돌 점수 배수        -- 임시 밸런스
    silverMultiplier = 2.0,   -- 은 래그돌 점수 배수        -- 임시 밸런스
    goldChance       = 0.05,  -- 스폰 시 금 변형 확률(0~1)  -- 임시 밸런스
    silverChance     = 0.10,  -- 스폰 시 은 변형 확률(0~1)  -- 임시 밸런스

    timeBonus       = 10,   -- 종료 시 남은 1초당 가산점 — 적용 시점 미정   -- 임시 밸런스
    overloadPenalty = 200,  -- 서버 과부하로 끝났을 때 감점 — 적용 시점 미정 -- 임시 밸런스
}
